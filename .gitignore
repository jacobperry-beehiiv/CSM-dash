node_modules
.next
.env
.env.local
.env.production
*.log
.vercel
.DS_Store
dist
out
tsconfig.tsbuildinfo

# Editor / agent state
.claude/

# Customer data (test fixtures + plaintext snapshots — never check in raw
# customer data). The ENCRYPTED snapshot IS committed; see snapshot-crypto.
data/*.csv
data/*.json
!data/.gitkeep
!data/snapshot.enc.json
